{
  "name": "lvivcssweb",
  "version": "0.0.0",
  "scripts": {
    "dev": "pug ./pug/*.pug -o ./ -O ./data/data.json -w",
    "start": "live-server",
    "deploy": "firebase deploy"
  },
  "private": true,
  "devDependencies": {
    "pug": "^2.0.4"
  },
  "dependencies": {
    "pug-cli": "^1.0.0-alpha6"
  }
}