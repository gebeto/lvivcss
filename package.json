{
  "name": "lvivcssweb",
  "version": "0.0.0",
  "scripts": {
    "hbs": "hbs -P './hbs/partials/*' -H ./hbs/helpers/*.js -D './data/data.json' './hbs/**.hbs'",
    "start": "live-server",
    "deploy": "firebase deploy"
  },
  "private": true,
  "dependencies": {
    "handlebars-helpers": "^0.10.0"
  }
}