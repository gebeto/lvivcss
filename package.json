{
  "name": "lvivcssweb",
  "version": "0.0.0",
  "scripts": {
    "hbs": "hbs -P './hbs/partials/*' -D './data/data.json' './hbs/index.hbs'",
    "start": "live-server",
    "deploy": "firebase deploy"
  },
  "private": true,
  "dependencies": {
    "handlebars-helpers": "^0.10.0"
  }
}
